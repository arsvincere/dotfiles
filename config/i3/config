# i3 config file (v4)
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

# Mod4 - Super
# Mod1 - Alt
set $mod Mod4
floating_modifier $mod
tiling_drag modifier titlebar
# Убрать рамки и строку заголовка окон
new_window pixel 0

# General bindings ------------------------------------------------------------
# use 'xmodmap -pke' for show all key codes

# start a terminal
bindsym $mod+space exec i3-sensible-terminal

# start rofi menu
# run in terminal 'rofi-theme-selector' for choose theme
bindsym $mod+Tab exec --no-startup-id rofi -show run

# betterlockscreen
# https://github.com/betterlockscreen/betterlockscreen
# yay -S betterlockscreen
exec --no-startup-id betterlockscreen -w blur 
bindsym $mod+Alt+l exec betterlockscreen -l blur

# Screenshoot (yay -S flameshot)
bindsym Print exec --no-startup-id flameshot full
bindsym Shift+Print exec --no-startup-id flameshot gui

# Toggle keylayot
# exec --no-startup-id setxkbmap -model pc105 -layout us,ru -option grp:caps_toggle,grp_led:caps,grp:switch
# exec --no-startup-id setxkbmap -model pc105 -layout us,ru -option grp:win_space_toggle
exec --no-startup-id setxkbmap -model pc105 -layout us,ru -option grp:alt_shift_toggle

# kill focused window
bindsym $mod+Escape kill

# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Ctrl+u restart

# exit i3 (logs you out of your X session)
bindsym $mod+Ctrl+Escape exec "i3-msg exit"

# lxappearange 
# Тема для thunar - меняется утилитой lxappearance
# ~ yay -S lxappearance 
# ~ lxappearance

# # Gaps
# gaps outer|horizontal|vertical|top|left|bottom|right 8[px]
# gaps left 10px
# gaps inner 10px
# # Overwrite gaps to 0, I need all the space I can get on workspace 3.
# workspace 0 gaps left 0
# workspace 0 gaps inner 0

# Auto start -----------------------------------------------------------------

# start polybar
exec_always --no-startup-id ~/.config/polybar/launch.sh

# Wallpaper
# yay -S nitrogen
# run in terminal 'nitrogen' for choose image
exec "--no-startup-id nitrogen --restore &"

# Transparent and other visual effect
# yay -S picom
# exec --no-startup-id nitrogen --restore; sleep 1; compton -b

# Start XDG autostart .desktop files using dex. See also
# https://wiki.archlinux.org/index.php/XDG_Autostart
exec --no-startup-id dex --autostart --environment i3

# autostart nvim
# exec "i3-msg 'workspace vim; exec alacritty -e nvim'"

# Switch ---------------------------------------------------------------------

# Define names for default workspaces for key bindings later
set $ws1 "avin"
set $ws2 "sh"
set $ws3 "web"
set $ws4 "dev"
set $ws5 "file"
set $ws6 "dir"
set $ws7 "write"
set $ws8 "read"
set $ws9 "xmind"
set $ws10 "telegram"
set $ws11 "pinta"
set $ws12 "inkscape"
set $ws13 "calc"
set $ws14 "v"
set $ws15 "b"
# switch to workspace
bindsym $mod+q      workspace "xmind"
bindsym $mod+w      workspace "web"
bindsym $mod+e      workspace "write"
bindsym $mod+r      workspace "read"
bindsym $mod+t      workspace "telegram"
bindsym $mod+a      workspace "avin"
bindsym $mod+s      workspace "sh"
bindsym $mod+d      workspace "dev"
bindsym $mod+f      workspace "file"
bindsym $mod+g      workspace "dir"
bindsym $mod+z      workspace "pinta"
bindsym $mod+x      workspace "inkscape"
bindsym $mod+c      workspace "calc"
bindsym $mod+v      workspace "v"
bindsym $mod+b      workspace "b"
bindsym $mod+less   workspace prev
bindsym $mod+greater  workspace next

# Bind program ---------------------------------------------------------------

bindsym $mod+Shift+q exec "i3-msg 'exec xmind'"
bindsym $mod+Shift+w exec "i3-msg 'exec google-chrome-stable'"
bindsym $mod+Shift+e exec "i3-msg 'exec obsidian'"
bindsym $mod+Shift+r exec "i3-msg 'exec evince'"
bindsym $mod+Shift+t exec "i3-msg 'exec telegram-desktop'"

bindsym $mod+Shift+a exec "i3-msg 'exec ya/avin-dev/main.py'"
bindsym $mod+Shift+s exec "i3-msg 'exec alacritty'"
bindsym $mod+Shift+d exec "i3-msg 'exec alacritty -e nvim'"
bindsym $mod+Shift+f exec "i3-msg 'exec alacritty -e vifm'"
bindsym $mod+Shift+g exec "i3-msg 'exec thunar'"

bindsym $mod+Shift+z exec "i3-msg 'exec pinta'"
bindsym $mod+Shift+x exec "i3-msg 'exec inkscape'"
bindsym $mod+Shift+c exec "i3-msg 'exec mate-calc'"

bindsym $mod+Alt+q exec "i3-msg 'workspace xmind; exec xmind'"
bindsym $mod+Alt+w exec "i3-msg 'workspace web; exec google-chrome-stable'"
bindsym $mod+Alt+e exec "i3-msg 'workspace write; exec obsidian'"
bindsym $mod+Alt+r exec "i3-msg 'workspace read; exec evince'"
bindsym $mod+Alt+t exec "i3-msg 'workspace telegram; exec telegram-desktop'"

bindsym $mod+Alt+a exec "i3-msg 'workspace avin; exec ya/avin-dev/main.py'"
bindsym $mod+Alt+s exec "i3-msg 'workspace sh; exec alacritty'"
bindsym $mod+Alt+d exec "i3-msg 'workspace dev; exec alacritty -e nvim'"
bindsym $mod+Alt+f exec "i3-msg 'workspace file; exec alacritty -e vifm'"
bindsym $mod+Alt+g exec "i3-msg 'workspace thunar; exec thunar'"

bindsym $mod+Alt+z exec "i3-msg 'workspace pinta; exec pinta'"
bindsym $mod+Alt+x exec "i3-msg 'workspace inkscape; exec inkscape'"
bindsym $mod+Alt+c exec "i3-msg 'exec mate-calc'"
# bindsym $mod+Shift+v exec "i3-msg 'workspace inkscape; exec inkscape'"
# bindsym $mod+Shift+b exec "i3-msg 'workspace inkscape; exec inkscape'"

# Split ----------------------------------------------------------------------

bindsym $mod+slash split h
bindsym $mod+m split v
bindsym $mod+y layout stacking
bindsym $mod+u layout tabbed
bindsym $mod+i layout toggle split
bindsym $mod+o floating toggle
bindsym $mod+p focus mode_toggle
bindsym $mod+braceleft mode "resize"
mode "resize" {
        bindsym h resize shrink width 1 px or 1 ppt
        bindsym j resize shrink height 1 px or 1 ppt
        bindsym k resize grow height 1 px or 1 ppt
        bindsym l resize grow width 1 px or 1 ppt
        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

# Focus ----------------------------------------------------------------------

bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right
bindsym $mod+Ctrl+h move left
bindsym $mod+Ctrl+j move down
bindsym $mod+Ctrl+k move up
bindsym $mod+Ctrl+l move right
bindsym $mod+Ctrl+o focus parent
bindsym $mod+Ctrl+i focus child

# Sound ----------------------------------------------------------------------

# Use pactl to adjust volume in PulseAudio.
set $refresh_i3status killall -SIGUSR1 i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +10% && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -10% && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status

